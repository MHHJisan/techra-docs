import { EditorDocument } from "../types/document";

export function exportDOCX(document: EditorDocument) {
  alert(
    `DOCX export for "${document.title}" will be implemented in Milestone 6.3`,
  );
}
